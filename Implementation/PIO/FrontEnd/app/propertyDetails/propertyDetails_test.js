'use strict';

describe('myApp.propertyDetails module', function() {

  beforeEach(module('myApp.propertyDetails'));

  describe('propertyDetails controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var propertyDetailsCtrl = $controller('PropertyDetailsCtrl');
      expect(propertyDetailsCtrl).toBeDefined();
    }));

  });
});

